;+
; NAME:
;	rad_mk
;
; PURPOSE:
;     >>>>>>>>>>>> turn absolute K mag into radius with errors<<<<<<<<<<<<<<
;
; CALLING SEQUENCE:
; 
;	rad_mk,mk,mk_err,rad,rad_err,feh=feh,feh_Err=feh_err
;
; INPUTS
;     mk = effective temperature
;     mk_err = error on teff
;     feh = metallicity (optional but improves precision)
;     feh_err = error on feh
;
; OUTPUTS:
;     rad = radius in solar units
;     rad_err = error on radius
;
;
; RESTRICTIONS:
;

PRO rad_MK


END
